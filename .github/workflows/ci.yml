# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [joseph, main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      # --------------------------------------------------------------
      # 1️⃣ Checkout the repository
      # --------------------------------------------------------------
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4

      # --------------------------------------------------------------
      # 2️⃣ Cache pip downloads (optional but speeds up CI)
      # --------------------------------------------------------------
      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # --------------------------------------------------------------
      # 3️⃣ Set up the Python version from the matrix
      # --------------------------------------------------------------
      - name: 🐍 Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # --------------------------------------------------------------
      # 4️⃣ Install runtime dependencies
      # --------------------------------------------------------------
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # --------------------------------------------------------------
      # 5️⃣ **Install linting tools**  ← **NEW SECTION**
      # --------------------------------------------------------------
      - name: 🔧 Install linting tools (ruff, black, flake8)
        run: |
          pip install ruff black flake8   # ruff covers most checks; black/flake8 optional

      # --------------------------------------------------------------
      # 6️⃣ **Run the linter**  ← **NEW SECTION**
      # --------------------------------------------------------------
      - name: 🧹 Run ruff (lint & auto‑fix check)
        run: ruff check .

      # --------------------------------------------------------------
      # 7️⃣ Optionally enforce Black formatting
      # --------------------------------------------------------------
      - name: 📏 Check Black formatting
        run: black --check .

      # --------------------------------------------------------------
      # 8️⃣ Optionally run Flake8 for extra style rules
      # --------------------------------------------------------------
      - name: 📐 Run Flake8
        run: flake8 .

      # --------------------------------------------------------------
      # 9️⃣ Install the repository in editable mode (now works!)
      # --------------------------------------------------------------
      - name: 📦 Install repo in editable mode
        run: |
          pip install -e .

      # --------------------------------------------------------------
      # 10️⃣ Validate selectors.yaml (your existing lint step)
      # --------------------------------------------------------------
      - name: ✅ Validate selectors.yaml
        run: |
          python -c "import yaml, sys; yaml.safe_load(open('configs/selectors.yaml')) or {}"

      # --------------------------------------------------------------
      # 11️⃣ Run the test suite
      # --------------------------------------------------------------
      - name: 🧪 Run tests
        run: pytest -q