{
  "title": "Overflow Web Scraper Input",
  "schemaVersion": 1,
  "type": "object",
  "properties": {
    "url": {
      "title": "Start URL",
      "type": "string",
      "description": "The URL to start crawling from (e.g., faculty page, directory).",
      "default": ""
    },
    "maxDepth": {
      "title": "Max Depth",
      "type": "integer",
      "default": 2,
      "description": "How deep to crawl (0 = single page, 1 = profile links, etc)."
    },
    "maxPages": {
      "title": "Max Pages",
      "type": "integer",
      "default": 50,
      "description": "Limit the number of pages crawled in total."
    },
    "campaign": {
      "title": "Campaign Name",
      "type": "string",
      "default": "default",
      "description": "Used to load selector pack (e.g., 'university_faculty', 'music_festivals')."
    }
  },
  "required": ["url"]
}
git add input_schema.json